import { AdminPage } from "@/components/admin/admin-page"; import { AdminCard } from "@/components/admin/admin-widgets"; import { NewPageSectionForm } from "@/components/admin/new-page-section-form";
export default async function NewSectionPage({ searchParams }: { searchParams: Promise<{ page?: string }> }) { const page = (await searchParams).page ?? "home"; return <AdminPage eyebrow="Pages" title="New page component" intro="Add a structured component, then attach uploaded images or videos and publish it."><AdminCard title="Component details" eyebrow="Draft"><NewPageSectionForm defaultPage={page} /></AdminCard></AdminPage>; }
