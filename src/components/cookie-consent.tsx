"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
const consentKey = "merostream-privacy-consent";
export function CookieConsent() { const [visible, setVisible] = useState(false); useEffect(() => { const frame = requestAnimationFrame(() => setVisible(localStorage.getItem(consentKey) !== "accepted")); return () => cancelAnimationFrame(frame); }, []); if (!visible) return null; return <aside role="dialog" aria-label="Privacy and cookie consent" className="fixed inset-x-4 bottom-4 z-[100] mx-auto max-w-3xl border border-sahel/35 bg-obsidian/95 p-5 pr-14 shadow-2xl backdrop-blur-xl md:flex md:items-center md:justify-between md:gap-6"><button type="button" onClick={() => setVisible(false)} aria-label="Close cookie notice without accepting" className="absolute right-3 top-3 inline-flex h-9 w-9 items-center justify-center rounded-full border border-papyrus/20 text-xl leading-none text-papyrus/70 transition hover:border-sahel hover:text-sahel">×</button><p className="text-sm leading-6 text-papyrus/75">We use essential cookies and, where enabled, performance cookies to improve MeroStream. Read our <Link href="/privacy-cookies-policy" className="text-sahel underline underline-offset-4">Privacy &amp; Cookies Policy</Link>.</p><button type="button" onClick={() => { localStorage.setItem(consentKey, "accepted"); setVisible(false); }} className="mt-4 min-h-11 shrink-0 rounded-full bg-sahel px-5 font-label text-xs font-bold uppercase tracking-[0.16em] text-obsidian md:mt-0">Accept</button></aside>; }
